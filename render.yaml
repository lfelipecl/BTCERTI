services:
  - type: web
    name: voila-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: main.ipynb --port=$PORT --no-browser --strip_sources=False
